apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
  namespace: ${NAMESPACE}
  labels:
    app: ${APP_NAME}
    service: ${APP_NAME}
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: ${PORT}
    protocol: TCP
  selector:
    app: ${APP_NAME}